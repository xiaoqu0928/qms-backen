<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.qms.mapper.QueryLogMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO query_log (student_id, student_name, query_time, ip_address, user_agent)
        VALUES (#{studentId}, #{studentName}, NOW(), #{ipAddress}, #{userAgent})
    </insert>
    <select id="findByStudentId" resultType="com.example.qms.entity.QueryLog">
        SELECT * FROM query_log WHERE student_id = #{studentId} ORDER BY query_time DESC
    </select>

    <!-- 新增 SQL -->
    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM query_log
    </select>
</mapper>